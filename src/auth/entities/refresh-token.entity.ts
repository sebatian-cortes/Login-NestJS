import {
    Column,
    Entity,
    PrimaryGeneratedColumn,
  } from 'typeorm';

@Entity('RefreshToken')
export class RefreshToken{
    // @PrimaryGeneratedColumn()
    // id_usuario: number;

    // @Column()
    // token: string;

    // @Column()
    // expiryDate: Date;
}